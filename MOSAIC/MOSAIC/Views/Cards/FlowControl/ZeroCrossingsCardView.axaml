<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:MOSAIC.Converters"
    xmlns:scope="clr-namespace:MOSAIC.Visualization.ScopeMonitor"
    xmlns:vm="clr-namespace:MOSAIC.ViewModels.FlowControl"
    xmlns:visualization="clr-namespace:MOSAIC.Visualization"
    x:Class="MOSAIC.Views.Cards.FlowControl.ZeroCrossingsCardView"
    x:DataType="vm:ZeroCrossingsViewModel">

    <UserControl.Resources>
        <conv:StatusToColorConverter x:Key="StatusToColor"/>
    </UserControl.Resources>

    <Border Classes="card">

        <StackPanel Spacing="12">

            <!-- Header -->
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Spacing="3">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="{Binding ZeroCrossings.Name}" Classes="cardTitle"/>
                        <Border Background="{StaticResource CategoryFlowControlBrush}" CornerRadius="4" Padding="6,2">
                            <TextBlock Text="ZC" FontSize="10" FontWeight="Bold" Foreground="White"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{Binding ZeroCrossings.FrequencyText}" Classes="cardSubtitle"/>
                        <TextBlock Text="•" Classes="cardSubtitle"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ZeroCrossings.OutputChannels}" 
                                       FontSize="11" Foreground="{StaticResource CategoryFlowControlBrush}" FontWeight="Medium"/>
                            <TextBlock Text=" ch × " FontSize="11" Foreground="{StaticResource CategoryFlowControlBrush}"/>
                            <TextBlock Text="{Binding ZeroCrossings.WindowSize}" 
                                       FontSize="11" Foreground="{StaticResource CategoryFlowControlBrush}" FontWeight="Medium"/>
                            <TextBlock Text=" samples" FontSize="11" Foreground="{StaticResource CategoryFlowControlBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" 
                        Background="{Binding ZeroCrossings.Status, Converter={StaticResource StatusToColor}}"
                        CornerRadius="12" Padding="12,5" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ZeroCrossings.Status}" 
                               FontWeight="SemiBold" Foreground="White" FontSize="10"/>
                </Border>
            </Grid>

            <Border Classes="divider"/>

            <!-- Info Section -->
            <Border Classes="cardSection">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" Spacing="2">
                        <TextBlock Text="FEATURE EXTRACTION" Classes="sectionLabel"/>
                        <TextBlock Text="Zero Crossings" FontSize="12" 
                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}" FontWeight="SemiBold"/>
                        <TextBlock Text="Counts signal sign changes (frequency indicator)" FontSize="10" 
                                   Foreground="{DynamicResource ThemeTextSecondaryBrush}" Margin="0,2,0,0"/>
                    </StackPanel>
                    
                    <!-- Formula display -->
                    <Border Grid.Column="1" Background="{DynamicResource ThemeInputBackgroundBrush}" 
                            CornerRadius="6" Padding="10,6">
                        <TextBlock Text="{Binding Description}" 
                                   FontSize="11" Foreground="{StaticResource CategoryFlowControlBrush}" 
                                   FontFamily="Consolas,Monaco,monospace"
                                   VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Configuration Section -->
            <Border Classes="cardSection">
                <StackPanel Spacing="10">
                    <TextBlock Text="CONFIGURATION" Classes="sectionLabel"/>

                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" RowSpacing="8" ColumnSpacing="12">
                        <!-- Threshold -->
                        <StackPanel Grid.Column="0" Grid.Row="0" Spacing="4">
                            <TextBlock Text="Noise Threshold" Classes="fieldLabel"/>
                            <Border Background="{DynamicResource ThemeInputBackgroundBrush}"
                                    CornerRadius="6" Padding="8,6"
                                    BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1">
                                <TextBox Text="{Binding ThresholdText, Mode=TwoWay}"
                                         BorderThickness="0" Background="Transparent"
                                         Foreground="{DynamicResource ThemeTextPrimaryBrush}" FontSize="12"
                                         PlaceholderText="0.0"
                                         ToolTip.Tip="Minimum amplitude change to count as zero crossing"/>
                            </Border>
                        </StackPanel>
                        
                        <!-- Info -->
                        <Border Grid.Column="1" Grid.Row="0" 
                                Background="{DynamicResource ThemeInputBackgroundBrush}" 
                                CornerRadius="6" Padding="12,8" VerticalAlignment="Center">
                            <StackPanel Spacing="2">
                                <TextBlock Text="THRESHOLD" Classes="sectionLabel" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding ZeroCrossings.Threshold, StringFormat={}{0:F3}}" 
                                           FontSize="14" Foreground="{StaticResource CategoryFlowControlBrush}" 
                                           FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Error message -->
                        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                                   Text="{Binding ErrorMessage}"
                                   FontSize="10" Foreground="{StaticResource StatusErrorBrush}"
                                   IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                   TextWrapping="Wrap"/>
                        
                        <!-- Help text -->
                        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2"
                                   Text="Higher threshold filters out small amplitude crossings (noise suppression)." 
                                   FontSize="9" Foreground="{DynamicResource ThemeTextSecondaryBrush}" TextWrapping="Wrap"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Scope Section -->
            <Border Classes="cardSection">
                <StackPanel Spacing="8">
                    <TextBlock Text="ZC OUTPUT" Classes="sectionLabel"/>

                    <Border Background="{DynamicResource ThemeScopeBackgroundBrush}" CornerRadius="6" ClipToBounds="True" MinHeight="155">
                        <visualization:VisualizationPanel Source="{Binding ZeroCrossings.Viz}" ScopeVisibleByDefault="True" 
                                                          ShowScopeViewModeToggle="True" MinHeight="160"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </Border>
</UserControl>